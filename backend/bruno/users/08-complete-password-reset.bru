meta {
  name: Complete Password Reset
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/api/auth/reset-password/complete
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "resetToken": "{{resetToken}}",
    "newPassword": "{{ownerNewPassword}}"
  }
}
