meta {
  name: Create User
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/api/auth/register
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "name": "bobo2",
    "email": "bobo2@gmail.com",
    "password": "Test2222!"
  }
}

script:post-response {
  const data = res.getBody();
  
  if (data?.id) {
    bru.setEnvVar("ownerUserId", data.id, { persist: true });
  }
}
